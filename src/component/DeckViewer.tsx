/* eslint-disable @typescript-eslint/no-unused-vars */

import { Deck } from "@/ontology";
import styles from "./DeckViewer.module.css";

export default function DeckViewer(props: { deck: Deck }) {
    return <div className={styles.DeckViewer}>
        {/* TODO: interactive viewer UI for deck */}
    </div>;
}
