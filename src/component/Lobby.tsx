import styles from "./Lobby.module.css";

export default function Lobby() {
    return (
        <div className={styles.Lobby}>
            {/* TODO: list of all games, which presents each game's name, status (whether "lobby" or "active"), member users, etc. */}
        </div>
    );
}
